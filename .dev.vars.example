# Rest-server access JWT secrets. Copy to .dev.vars for local wrangler.
# Local worker only needs JWT_SECRET (match local rest-server).
JWT_SECRET=change-me-local
# JWT_SECRET_STAGING=change-me-staging
